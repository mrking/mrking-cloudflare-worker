name = "mrking-cloudflare-worker"   # or whatever your Worker is called
main = "src/email-worker.js"
compatibility_date = "2025-12-06"

# This is an email worker; the actual email routing is configured in the UI.
